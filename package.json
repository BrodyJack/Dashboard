{
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd dashboard_server && npm start",
    "client": "cd dashboard_app && npm start",
    "install-all": "npm install && cd dashboard_app && npm install && cd ../dashboard_server && npm install && cd ../"
  },
  "devDependencies": {
    "concurrently": "^3.6.0"
  }
}
